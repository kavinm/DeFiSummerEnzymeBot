import React from 'react';
import DefaultLayout from '../layouts/DefaultLayout';

const RebalancePortfolio: React.FC = () => {
  return <DefaultLayout name="Rebalance Portfolio">hey</DefaultLayout>;
};

export default RebalancePortfolio;
