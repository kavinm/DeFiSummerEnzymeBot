import React from 'react';
import DefaultLayout from '../layouts/DefaultLayout';

const Liquidate: React.FC = () => {
  return <DefaultLayout name="Liquidate">hey</DefaultLayout>;
};

export default Liquidate;
